# Tablo DVR Configuration
TABLO_IPS=192.168.1.100

# Show to download (optional - leave empty for full dump)
SHOW_MATCH=

# Put.io OAuth Token (get from https://app.put.io/account/api)
PUTIO_TOKEN=your-putio-oauth-token-here

# Debug mode (set to true for verbose logging)
DEBUG=

# Tailscale auth key (optional, for reaching Tablo over Tailscale)
TS_AUTHKEY=
TS_EXTRA_ARGS=--accept-routes
TS_UP_RESET=true
TS_TUN=tailscale0
TAILSCALED_ARGS="--statedir=/var/lib/tailscale --socket=/var/run/tailscale/tailscaled.sock --tun=${TS_TUN}"
TAILSCALE_WAIT_SECS=60
